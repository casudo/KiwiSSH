### EdgeCore OS Vendor Configuration
## Instructions for backing up EdgeCore OS

vendor:
  id: "edgecore_edgecos"
  name: "EdgeCore EdgeCOS"
  description: "EdgeCore EdgeCOS network devices"

### Session-level output behavior.
session:
  comment_prefix: "! "
  pagination:
    enabled: true
    patterns:
      - pattern: '^---More---.*$'
        response: " "

### Commands to execute for backup
## Supported step types in command phases:
## - command: sends command text
## - command + then: sends command text, then sends interactive input
commands:
  ### Pre-backup commands (prepare the session) (OPTIONAL)
  ## Command outputs aren't saved in stored configuration
  pre_backup:
    - command: "enable"
    - command: "terminal length 0"
    - command: "terminal width 300"

  ### Main backup command(s)
  ## Commands whose output becomes the stored configuration
  backup:
    - command: "show version"
      metadata: true
    - command: "show watchdog"
      metadata: true
    - command: "show interface transceiver"
      metadata: true
    - command: "show running-config"
      show_command_in_config: true

  ### Post-backup commands (cleanup) (OPTIONAL)
  ## Command outputs aren't saved in stored configuration
  post_backup:
    - command: "exit"
    - command: "exit"

### Configuration processing rules
## Processing reduces diff noise and trims non-config boilerplate
processing:
  strip_patterns:
    - '^(% Invalid input detected at'
    - '^.*\sUp Time\s*:.*\n'
    - '(\sTemperature \d*:)\s*\d+ degrees'
    - 'Fan speed'
  # config_start: "^config "
  # config_end: "^end$"
  ### Secret masking. When enabled=false, no redaction is applied.
  redaction:
    enabled: false
    patterns:
      - search: 'password \d+ (\S+).*'
        replacement: '<secret hidden>'
      - search: 'community (\S+).*'
        replacement: 'community <hidden>'
