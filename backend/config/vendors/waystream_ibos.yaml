### Waystream (PacketFront) iBOS (Intelligent Broadband OS) Vendor Configuration

vendor:
  id: "waystream_ibos"
  name: "Waystream iBOS"
  description: "Used in Waystream (previously PacketFront) Routers and Switches"

### Session-level output behavior.
session:
  comment_prefix: "! "
  prompt: '^([\w.@()-]+[#>]\s?)$'

### Commands to execute for backup
## Supported step types in command phases:
## - command: sends command text
## - command + then: sends command text, then sends interactive input
commands:
  ### Pre-backup commands (prepare the session) (OPTIONAL)
  ## Command outputs aren't saved in stored configuration
  pre_backup:
    - command: "enable"
      then: ["{{ enable_password }}"]
    - command: "terminal no pager"
    - command: "terminal width 65535"

  ### Main backup command(s)
  ## Commands whose output becomes the stored configuration
  backup:
    - command: "show version"
      metadata: true
    - command: "show running-config"
      show_command_in_config: true

  ### Post-backup commands (cleanup) (OPTIONAL)
  ## Command outputs aren't saved in stored configuration
  post_backup:
    - command: "exit"

### Configuration processing rules
## Processing reduces diff noise and trims non-config boilerplate
processing:
  strip_patterns:
    - '.*!volatile.*'
  # config_start: "^config "
  # config_end: "^end$"
  ### Secret masking. When enabled=false, no redaction is applied.
  redaction:
    enabled: false
    patterns:
      - search: '^ notify (\S+) community (\S+) (.*)'
        replacement: ' notify \1 community <hidden> \3'
      - search: '^ community (\S+) (.*)'
        replacement: ' community <hidden> \2'
      - search: '^radius server (\S+) secret (\S+)(.*)'
        replacement: 'radius server \1 secret <hidden> \3'
